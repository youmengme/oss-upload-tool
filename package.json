{
  "name": "oss-action",
  "version": "1.0.25",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/index.js -m -o ./dist",
    "deploy": "yarn build && git add . && git commit -m 'update' && npm version patch && git push --tags"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@vercel/ncc": "^0.26.1",
    "ali-oss": "^6.12.0"
  }
}
